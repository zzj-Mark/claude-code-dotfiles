# Chezmoi configuration for Claude Code dotfiles

# Default GitHub configuration
[github]
  user = "zzj-Mark"

# Template variables
[data]
  # Your Anthropic API base URL (change if using a proxy)
  anthropicBaseUrl = "https://open.bigmodel.cn/api/anthropic"
